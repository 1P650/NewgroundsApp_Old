<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    tools:parentTag="com.google.android.material.card.MaterialCardView">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/dashboard_label"
        android:layout_width="match_parent"
        android:layout_height="@dimen/dashboard_labelSize"
        android:background="@drawable/dashboard_label_gradient"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/dashboard_label_icon"
            android:layout_width="@dimen/dashboard_iconSize"
            android:layout_height="@dimen/dashboard_iconSize"
            android:layout_marginStart="8dp"
            android:padding="2dp"
            android:background="@drawable/dashboard_icon_shape"
            android:tint="@color/colorAccent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:srcCompat="@drawable/ng_default_icon" />

        <TextView
            android:id="@+id/dashboard_label_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="8dp"
            android:text="@string/LabelNameDefault"
            android:textSize="@dimen/dashboard_textSize"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toEndOf="@+id/dashboard_label_icon"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintVertical_bias="0.428" />

        <LinearLayout
            android:id="@+id/dashboard_buttons_panel"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:clickable="true"
            android:divider="@drawable/dashboard_divider"
            android:orientation="horizontal"
            android:showDividers="middle"
            android:gravity="end"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            android:paddingRight="12dp"
            android:paddingLeft="12dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.0"
            app:layout_constraintStart_toEndOf="@+id/dashboard_label_name"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintVertical_bias="0.0" />
    </androidx.constraintlayout.widget.ConstraintLayout>

</merge>